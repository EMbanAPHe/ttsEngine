# Use AndroidX libraries (required because dependencies are AndroidX)
android.useAndroidX=true
# Jetifier helps migrate old support libs if any transitive dep still references them
android.enableJetifier=true
# Suppress AGP 8.2.2 warning when using compileSdk=35
android.suppressUnsupportedCompileSdk=35
